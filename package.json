{
  "name": "vehicle-side-scroller",
  "version": "1.0.0",
  "description": "A Phaser 3 side-scroller game with vehicle shrinking/growing mechanics",
  "type": "module",
  "scripts": {
    "generate-manifest": "node generate-assets-manifest.js",
    "dev": "npm run generate-manifest && vite",
    "build": "npm run generate-manifest && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.1.1",
    "terser": "^5.44.1",
    "typescript": "^5.9.3",
    "vite": "^5.0.8",
    "vitest": "^4.0.16"
  }
}
